
@{
    ViewBag.Title = "categoryBasedProduct";
}


<!-- start New Arrival -->
<section id="mainContent">

    <div class="container">
        <h2>Category Based</h2>
        <div class="row">

            <div class="col-md-12">
                @{
                    if (ViewBag.productList.Length == 0)
                    {
                        <h3>No Product found...</h3>
                    }
                    else
                    {
                        int count = 0;
                        foreach (var element in ViewBag.productList)
                        {
                            count++;
                            if (count <= 3)
                            {
                                <div class="col-md-4">
                                    <div class="myProduct">
                                        <a href="#">
                                            @if (@element.imageAddress != null)
                                            {
                                                <img src="@Url.Content(element.imageAddress)" />
                                            }
                                            else
                                            {
                                                <img src="@Url.Content(ViewBag.defaultImageAddress)" />
                                            }

                                        </a>
                                        <div class="productBottom">
                                            <span class="productPrice">Rs. @Convert.ToInt32(element.sellingPrice)</span>
                                            <a class="addToCart" href="#">Add to Cart</a>
                                        </div>
                                    </div>
                                </div>
                            }

                        }
                    }
                }


            </div>
        </div>
    </div>
</section>
<!-- end New Arrival -->

